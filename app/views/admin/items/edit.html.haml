.admin-items-edit-container
  %h3 Update Item

  %article.form-fields 
    = bootstrap_form_for [:admin, @item] do |f|

      %section.form-field
        = f.text_field :name, hide_label: true, placeholder: 'name'

      %section.form-field
        = f.text_field :description, hide_label: true, placeholder: 'description'

      %section.form-field
        = f.submit 'Update'